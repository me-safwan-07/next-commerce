// lib/constants.ts or types/category.ts (wherever you keep enums)

export enum Categorys {
  Electronics = "Electronics",
  Fashion = "Fashion"
}

export enum Status {
    inStock = "In Stock",
    outStock = "Out Stock",
}
